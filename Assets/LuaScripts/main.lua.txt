print('hello world')

require("Ability")

print("--加载ConfigAbility--")
local configAbility_Weapon = require("ConfigAbility_Weapon")
local configAbility_Disc = require("ConfigAbility_Disc")
local configAbility_Role = require("ConfigAbility_Role")

configAbilities = {}

for k, v in pairs(configAbility_Weapon) do
    configAbilities[k] = v
end

for k, v in pairs(configAbility_Disc) do
    configAbilities[k] = v
end

for k, v in pairs(configAbility_Role) do
    configAbilities[k] = v
end